name: NoPlayerNotifier
version: ${project.version}
main: net.quickwrite.noplayernotifier.NoPlayerNotifier
description: A simple plugin that allows the server to notify a player when they are talking to themselves.
